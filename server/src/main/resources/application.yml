server:
  port: 8080

spring:
  #启用哪一个环境配置
  profiles:
    active: dev
  #开启循环依赖
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://${rbac.datasource.host}:${rbac.datasource.port}/${rbac.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: ${rbac.datasource.driver-class-name}
    username: ${rbac.datasource.username}
    password: ${rbac.datasource.password}
    druid:
      initial-size: 20
      min-idle: 20
      max-active: 100
      max-wait: 10000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 30000
      validation-query: SELECT 1 FROM DUAL
      test-while-idle: true
      test-on-borrow: true
      test-on-return: false
  #redis配置信息
  data:
    redis:
      host: ${rbac.redis.host}
      password: ${rbac.redis.password}
      port: ${rbac.redis.port}

  jackson:
    date-format: java.text.SimpleDateFormat
    time-zone: GMT+8

jwt:
    #管理端jwt配置
    # 设置jwt签名加密时使用的秘钥
  admin-secret-key: "ginwind_jwt_secret_key_1234567890123456"
    # 设置jwt过期时间
  admin-ttl: 3600000
    # 设置前端传递过来的令牌名称
  admin-token-name: token
    # Token存储在redis里的键
  token-redis-key: "token_"


  # 用户端jwt配置
    # 设置jwt签名加密时使用的秘钥
  user-secret-key: itheima
    # 设置jwt过期时间
  user-ttl: 7200000
    # 设置前端传递过来的令牌名称
  user-token-name: authentication

logging:
  level:
    com:
      ginwind:
        springrbac:
          mapper: debug
          service: info
          controller: info
